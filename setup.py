#!/usr/bin/env python

from distutils.core import setup


setup(name='q1logic',
      version='1.0',
      entry_points={
          'console_scripts': [
          ]
      },
      description='Q1 logic',
      install_requires=[],
      author='Matt Earl',
      packages=['q1logic'])

